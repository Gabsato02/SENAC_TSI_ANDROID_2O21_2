/*
ESTRUTURA DAS PASTAS

app
    manifests --onde ficam as configurações gerais do aplicativo
        xmlns --import de definição de xml
        package --pacote principal da aplicação, onde ficam as atividades
        application --configurações base da aplicação
    java
        br --arquivos da aplicação
        br (androidTest) --testes de integração
        br (test) --testes unitários
    java (generated) --configurações do Java, pouco usado
    res
        drawable --montagem de svgs
        mipmap --variação do drawable voltada pra responsividade
        values --variáveis globais
Gradle Scripts --similar ao npm
    build.gradle (Module) --configurações similares a um Package.json


DEFINIÇÕES
view - componentes
layout - estrutura
activity - módulo único e independente relacionado a uma tela de interface de usuário
e suas funcionalidades correspondentes
















*/